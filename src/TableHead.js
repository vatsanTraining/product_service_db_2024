import React from "react";

const TableHead = ({ colRef }) => {
  return (
      <thead>
        <tr>
          <th>{"Customer Name"}</th>
        </tr>
      </thead>
  );
};

export default TableHead;
